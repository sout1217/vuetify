import { DefaultLayout } from '@/router/default'
import { AuthenticationLayout } from '@/router/authentication'
import { PageLayout } from '@/router/page'

const routes = [DefaultLayout, AuthenticationLayout, PageLayout]

export default routes
